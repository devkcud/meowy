-> import : {
    -> aliases : {
        -> wire : "./codec"
    }
}

-> export : {
    -> packet : @"wire/header.mwy"
}

-> build : {
    -> entry : "./main.mwy"
    -> profile : "debug"
}

-> gatostyle : {
    -> preset : "structured"
}
