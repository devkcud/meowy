# requires --dynamic-mod compiler flag #

cb : "codeberg.org/meowy-lang/"

dependency : (name <string>) {
    -> fetch : cb + name
    -> ref : "main"
}

-> import : {
    -> rl : dependency("raylib")
    -> debug : dependency("debug")
}
