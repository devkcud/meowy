pato $ meowy run examples/main.mwy

meowy v0.0.1
running examples/main.mwy

1 - error[E103]: list capacity exceeded
  --> examples/main.mwy:3:13
   |
 1 | list: <string[3]> = ["A", "B", "C"]
   |       ----------- limited to 3 items
 2 |
 3 | list = list.add("D")
   |             ^^^^^^^^ cannot add a fourth item
   |
   = attempted to insert `"D"`

fix 1.2 [recommended]: increase capacity from 3 to 4
   |
 1 | list: <string[4]> = ["A", "B", "C"]
   |               ~
   |
   = `meowy err fix 1.2`

fix 1.1: remove the capacity limit
   |
 1 | list: <string[]> = ["A", "B", "C"]
   |              ~
   |
   = `meowy err fix 1.1`
   . introduces behavioral changes!


2 - error[E207]: incompatible assignment
  --> examples/main.mwy:7:8
   |
 7 | age: <int32> = "twenty"
   |      -------   ^^^^^^^^ expected <int32>, found <string>
   |
   = `"twenty"` cannot be assigned to <int32>

fix 2.1 [recommended]: use an integer value found by context
   |
 7 | age: <int32> = 20
   |                ~~
   |
   = `meowy err fix 2.1`

fix 2.2: use an integer value
   |
 7 | age: <int32> = 0
   |                ~
   |
   = `meowy err fix 2.2`


3 - error[E103]: list capacity exceeded [same as 1]
  --> examples/main.mwy:12:17
   |
10 | names: <string[2]> = ["pato", "ari"]
   |        ----------- limited to 2 items
11 |
12 | names = names.add("cornelius")
   |               ^^^^^^^^^^^^^^^^ cannot add a third item
   |
   = attempted to insert `"cornelius"`

fix 3.2 [recommended]: increase capacity from 2 to 3
   |
10 | names: <string[3]> = ["pato", "ari"]
   |                ~
   |
   = `meowy err fix 3.2`

fix 3.1: remove the capacity limit
   |
10 | names: <string[]> = ["pato", "ari"]
   |               ~
   |
   = `meowy err fix 3.1`
   . introduces behavioral changes!


3 errors emitted; each error was saved as an isolated reproducible artifact under /home/pato/.cache/meowy/-home-pato-examples-main.mwy/proj-1788649910/

reproduce:
   `meowy err reproduce 1`   reproduce error 1 verbosely [E103]
   `meowy err reproduce 2`   reproduce error 2 verbosely [E207]
   `meowy err reproduce 3`   reproduce error 3 verbosely [E103]

hint: run `meowy err fix all --diff --recommended` to inspect all (5) available fixes not found in this run.
      run `meowy err cleanup` to remove saved artifacts.
      run `meowy err explain 1` to get a detailed description on the error.

hint: quite a lot, huh? run `meowy err summary` to view less information.

found a bug in the compiler? psst@did.cat or https://meowy.did.cat/bug or `meowy err report 2` (requires login)

pato $ meowy err summary

meowy v0.0.1

3 errors from the last run:

1 - E103: list capacity exceeded
    examples/main.mwy:3:13
    2 fixes available (1.2 recommended)

2 - E207: incompatible assignment
    examples/main.mwy:7:8
    1 fix available (2.1 recommended)

3 - E103: list capacity exceeded (same as 1)
    examples/main.mwy:12:17
    2 fixes available (3.2 recommended)

3 errors - 5 fixes - 3 reproducible artifacts

`meowy err reproduce <id>` - `meowy err explain <id>` - `meowy err fix <id.fix>`

pato $ meowy err explain 1

Error 1 (E103) occurred because `list` was declared as a
`<string[3]>`, which allows at most three string values.

At line 3, the program calls `list.add("D")`. At this point the
list already contains three values, so adding `"D"` would require
a fourth slot that the declared type does not provide.

The capacity was declared here:

  examples/main.mwy:1:7
  list: <string[3]>

The operation that exceeded it occurred here:

  examples/main.mwy:3:13
  list.add("D")

There are two known fixes:

1. Increase the declared capacity from 3 to 4.
   This is the recommended fix.

2. Remove the fixed capacity and use `<string[]>`.
   This changes the behavior of the list by allowing its size
   to grow dynamically.

The original failure has been preserved and can be reproduced
in isolation with:

  meowy err reproduce 1

pato $ meowy err fix all --previous --diff --recommended

meowy v0.0.1

3 errors from the last run
3 recommended fixes selected

fix 1.2 [recommended]
E103: list capacity exceeded
examples/main.mwy:1

  increase capacity from 3 to 4

--- examples/main.mwy
+++ examples/main.mwy
@@ -1,4 +1,4 @@
-list: <string[3]> = ["A", "B", "C"]
+list: <string[4]> = ["A", "B", "C"]

 list = list.add("D")


fix 2.1 [recommended]
E207: incompatible assignment
examples/main.mwy:7

  use an integer value found by context

--- examples/main.mwy
+++ examples/main.mwy
@@ -7,1 +7,1 @@
-age: <int32> = "twenty"
+age: <int32> = 20


fix 3.2 [recommended]
E103: list capacity exceeded [same as 1]
examples/main.mwy:10

  increase capacity from 2 to 3

--- examples/main.mwy
+++ examples/main.mwy
@@ -10,4 +10,4 @@
-names: <string[2]> = ["pato", "ari"]
+names: <string[3]> = ["pato", "ari"]

 names = names.add("cornelius")


3 fixes selected
1 file would be changed

examples/main.mwy
  3 insertions
  3 deletions

no changes were written.

apply with:
  `meowy err fix all --previous --recommended`

or apply exactly these fixes with:
  `meowy err fix 1.2 2.1 3.2`

pato $ meowy err reproduce 1 --verbose

meowy v0.0.1
reproducing error 1 [E103]

artifact proj-1788649910/error-1

created:
  2026-09-05 20:41:50 -0300

original:
  /home/pato/examples/main.mwy
  line 3, column 13

compiler:
  version       0.0.1
  commit        4e91c72
  profile       debug
  executable    /home/pato/.local/bin/meowy
  sha256        85f47a...c921

target:
  triple        x86_64-unknown-linux-gnu
  cpu           x86-64
  features      +sse2,+sse4.2
  llvm          21.1.0
  linker        ld.lld

preserved files:
  source/main.mwy
  linked/main.ll
  linked/main.o
  binary/main
  build/Makefile
  build/llvm.stats
  build/ccache.stats
  build/commands.json
  build/environment
  stderr
  stdout
  manifest.json

verifying artifact...

  source/main.mwy       sha256 91c0...72a1  ok
  linked/main.ll        sha256 e27d...91ca  ok
  linked/main.o         sha256 a882...07ef  ok
  binary/main           sha256 0d19...f5c2  ok
  build/Makefile        sha256 a7e3...112a  ok

artifact integrity: ok

restoring preserved environment...

ccache:
  cache directory       .artifact/ccache
  hits                  14
  misses                3
  cache size            812 KiB

link:
  ld.lld main.o -o main ...

executing:
  ./binary/main

--- reproduced stderr ---

1 - error[E103]: list capacity exceeded
  --> examples/main.mwy:3:13
   |
 1 | list: <string[3]> = ["A", "B", "C"]
   |       ----------- limited to 3 items
 2 |
 3 | list = list.add("D")
   |             ^^^^^^^^ cannot add a fourth item
   |
   = attempted to insert `"D"`

--- process ---

exit status: 1
elapsed:     1.82ms
signal:      none

reproduction complete.
