-> import : {
    -> debug : {
        -> fetch : "codeberg.org/meowy-lang/debug"
        -> tag : "v2.0.1"
    }

    -> cli : {
        -> fetch : "codeberg.org/meowy-lang/terminal"
        -> tag : "v1.0.0"
    }

    -> strings : {
        -> fetch : "codeberg.org/meowy-lang/strings"
        -> tag : "v3.4.1"
    }
}
